# Accès à l'espace Réalisations.
# Génère les deux valeurs avec :  node scripts/generer-acces.mjs "ton-mot-de-passe"
ACCES_HASH="scrypt:<sel>:<empreinte>"

# Secret de signature du cookie de session (32 octets aléatoires en hexadécimal) :
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ACCES_SECRET="..."
